import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  capsule: {
    borderWidth: 1,
    borderColor: 'green',
    borderRadius: 12,
    paddingHorizontal: 8,
    paddingVertical: 4,
  },
  text: {
    fontSize: 12,
    color: 'green',
    textAlign: 'center',
  },
});
